/**
 * UI prototype boundary (Phase 2). Integrators: replace mocks with PlatformClient + Firebase
 * starting at chunk lock-in; do not import production API clients from this folder.
 */
export const UI_PROTOTYPE_BOUNDARY = 'ui-prototype' as const;
